/// <reference path="./show.ts" />

import {TaskShow} from './show';

describe('Task Show', () => {

    beforeEach(() => {
        let taskShow = new TaskShow(null);
    });

    it('test', () => {
        expect(true).toBe(true);
    });
});