/**
 * Use local.env.js for environment variables required through the application. This file
 * should not be tracked by your VCS and it's ignored by default in .gitignore
 * @type {{}}
 */
export const ENV = {

};
